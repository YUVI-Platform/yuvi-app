"use client";

import { LoginForm } from "@/components/ui/Forms/LoginForm";

export default function LoginPage() {
  return (
    <main className="w-screen h-screen max-h-screen max-w-screen flex justify-center items-center bg-background p-4">
      <LoginForm />
    </main>
  );
}
